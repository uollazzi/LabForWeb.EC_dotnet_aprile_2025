@model IEnumerable<ProdottoModel>

<div class="row row-cols-1 row-cols-md-4 g-4">
    @foreach (var p in Model)
    {
        <div class="col">

            <div class="card">
                <div class="card-body">
                    <h5></h5>
                </div>
            </div>
        </div>
    }
</div>